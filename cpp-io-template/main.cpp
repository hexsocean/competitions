#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("{[.NAME.]}in.txt", "r", stdin);
    freopen("{[.NAME.]}out.txt", "w", stdout);
    int take = 0;
    cin >> take;
    for (int i = 0; i < take; i++)
    {
        char temp;
        cin >> temp;
        cout << temp;
    }
}